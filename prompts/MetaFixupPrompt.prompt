Act as a JSON Validator.
Rules:
1. If "frequency" > 2.0, set to 2.0.
2. If "rotation_range" span > 60, reduce to [-15, 15].
3. Ensure all keys from the schema are present.
Output: The corrected JSON.
