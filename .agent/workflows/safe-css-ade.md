---
description: Run the Safe CSS validation workflow
---

This workflow runs the `safe_css.sh` script to validate visual regression tests and ensure system health.

1. Run the Safe CSS script
// turbo
```bash
./agent_env/bin/ADE_safe_css.sh
```
